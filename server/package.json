{
  "dependencies": {
    "@aws-sdk/client-s3": "^3.749.0",
    "dotenv": "^16.4.7",
    "socket.io": "^4.8.1",
    "winston": "^3.17.0"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=production nodemon --exec node --no-warnings --es-module-specifier-resolution=node --loader ts-node/esm server.ts",
    "dev": "cross-env NODE_ENV=development nodemon --exec node --no-warnings --es-module-specifier-resolution=node --loader ts-node/esm server.ts"
  },
  "type": "module",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  }
}
